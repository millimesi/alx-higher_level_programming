#!/usr/bin/python3
"""
Module with function"""


def is_same_class(obj, a_class):
    """ check if an object is exactly a of the class
    """
    if type(obj) is a_class:
        return True
    else:
        return False
